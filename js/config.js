/**
 * Optional: set the online server URL when the game is hosted elsewhere (e.g. GitHub Pages)
 * and the server is at a different origin.
 *
 * For production: set this to your deployed server (e.g. https://your-app.railway.app).
 * Leave as null to use same-origin (when loading from the server) or localhost (when loading locally).
 */
window.SOCKET_URL = window.SOCKET_URL || null;
